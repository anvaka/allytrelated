import React from 'react'
import ReactDOM from 'react-dom'
import Home from './home/HomeView.js'

// Render the React application to the DOM
ReactDOM.render(
  <Home />,
  document.getElementById('root')
)
